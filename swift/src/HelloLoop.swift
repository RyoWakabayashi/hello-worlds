var worldList = ["a", "b", "c"]

for world in worldList {
  print("world is \(world)!")
}

var worldDict = [
  "a": "aaa",
  "b": "bbb",
  "c": "ccc"
]

for (key, value) in worldDict {
  print("world \(key) is \(value)!")
}
